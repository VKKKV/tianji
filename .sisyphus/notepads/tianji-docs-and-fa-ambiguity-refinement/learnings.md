2026-03-23 Task 1: Branch setup can proceed safely by capturing dirty-state evidence first, then switching branches without staging or editing unrelated files.
2026-03-23 Task 2: Roadmap docs should distinguish shipped TUI behavior from future expansion by anchoring claims to cli.py command entrypoints and tui.py interaction states, especially compare staging and read-only persisted browsing.
2026-03-23 Task 3: Product-facing docs stay clearer when README treats `tianji/storage.py` as the persisted truth boundary, then explains `history-show` and `history-compare` filters as read-time lenses instead of restating every flag as a separate feature bullet.
2026-03-23 Task 3: `TUI_CONTRACT.md` reads best when it separates shipped read-only behavior from later expansion explicitly, using `tianji/tui.py`, `tianji/cli.py`, and `tests/test_tui.py` as proof anchors.
2026-03-23 Task 4: `SCORING_SPEC.md` stays audit-friendly when the shipped `Fa` contract is stated once as the complete rule set, then the next refinement is gated behind one canonical synthetic mixed-field proof rather than open-ended future wording.
2026-03-23 Task 5: The current `Fa` suite already covers the meaningful single-event mixed-field shapes in scope: isolated margin, isolated coherence, near-tie ambiguity, diffuse third-field support, zero-field mass, and deterministic top-field ties.
2026-03-23 Task 5: A synthetic search that keeps `Im` fixed can still surface tiny residual inversions at sub-threshold field mass, but a `0.02` `Fa` delta at `6.5/0.2/0.0` versus `6.5/0.1/0.1` is too weak to count as the canonical surviving ambiguity proof required to justify another rule.
2026-03-23 Task 8: Post-change doc cleanup stays honest when README, DEV_PLAN, SESSION_HANDOFF, and SCORING_SPEC all repeat the same stop condition explicitly: Task 5 proved no meaningful uncovered mixed-field `Fa` weakness, so no new `Fa` rule landed and the branch ends at docs alignment plus the no-gap proof.
2026-03-23 Task 9: Final docs-only verification is safest when evidence records both the passing test/help surfaces and the deliberate exclusion of unrelated scoring-code edits from the commit scope.
2026-03-23 Task 10: Click help output stays user-facing correct only when `tianji/cli.py` sets `prog_name` to the actual module invocation, because subcommand help inherits that entrypoint label automatically.

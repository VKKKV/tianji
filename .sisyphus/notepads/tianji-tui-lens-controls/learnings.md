2026-03-23: Branch setup for this plan should record the starting branch, short HEAD, and `git status --short` before switching so later tasks can distinguish inherited workspace state from task-specific changes.
2026-03-23: HistoryListState lens helpers should invalidate both cached detail and compare payloads through one shared method so selection, compare staging, active view, and focus semantics stay untouched while projected panes refresh.
2026-03-23: In the raw-key TUI, non-conflicting lens controls can stay discoverable by using single-letter cycles like a/s/d/f/v, then mirroring the active projection in both the header and footer so narrowed detail and compare panes explain themselves without adding text entry.
2026-03-23: TUI docs stay clearer when they separate shipped read-only browsing from the next planned Phase 5 slice, then name the five shared detail/compare lenses explicitly and defer list-pane filtering plus numeric threshold entry in the same section.
2026-03-23: Detail-pane cache invalidation is safest when the cache key includes both run_id and the active five-lens tuple, so same-run projection changes trigger fresh storage reads instead of stale detail lines.
2026-03-23: Compare-pane caching should mirror detail-pane projection semantics by keying cached compare lines on both right_run_id and the shared five-lens tuple, otherwise active lens changes can silently reuse stale compare output.
2026-03-23: TUI lens regression tests stay aligned with storage semantics by asserting exact `get_run_summary()` / `compare_runs()` kwargs and treating empty projected detail/compare payloads as valid operator views, matching the history-show/history-compare contract instead of redefining it.
2026-03-23: Operator-visible TUI copy stays clearer when header, footer, help, and minibuffer all use the same singular `lens:` vocabulary, then explicitly say the list remains persisted truth while detail and compare are projected views.
2026-03-23: Final manual QA is easiest against `runs/tianji.sqlite3` because two persisted runs are enough to prove list-pane truth, compare staging, compare-target bounds, zoom/focus/help behavior, and live lens summaries without inventing synthetic data.

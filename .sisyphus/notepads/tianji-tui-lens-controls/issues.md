2026-03-23: The workspace was already dirty before branch creation with several untracked `.sisyphus/` files, so that state was surfaced in `.sisyphus/evidence/task-1-branch-setup-error.txt` instead of being hidden.
2026-03-23: Direct `python -m unittest tests.test_tui -v` fails in this repo because `tests/test_tui.py` imports `support` as a top-level module, so targeted verification should use `python -m unittest discover -s tests -p "test_tui.py" -v`.
2026-03-23: Markdown LSP is unavailable here, so doc-only scope checks need excerpt-based evidence that proves planned wording, deferred threshold entry, and the absence of list-pane filtering claims.
2026-03-23: Rich Panel.renderable is typed broadly enough that TUI tests should cast to Text before reading `.plain`, otherwise Pyright reports attribute-access errors even when the runtime renderable is a Text instance.
2026-03-23: Tests that seed cached compare lines now also need to seed cached_compare_lens_key; otherwise build_compare_panel() correctly treats the cache as stale and refreshes instead of showing the seeded scroll window.
2026-03-23: Layout-level and browser-loop TUI tests must mock storage reads because `build_layout()` renders the active right pane; otherwise tests that only mean to inspect header/footer or key handling can accidentally open a real SQLite path and fail with `no such table: runs`.
2026-03-23: Compact footer tests can fail after copy polish if the width is tight enough to trigger `format_status_footer()` fallback text, so assertions should pin the stable state tokens that survive both full and compact footer rendering.
2026-03-23: The `script` transcript captures raw ANSI screen updates from the Rich TUI, so Task 9 evidence should treat the transcript as operator-session proof and use `tmux capture-pane` snapshots to inspect specific checkpoints during manual QA.

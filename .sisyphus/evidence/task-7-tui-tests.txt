Task 7 evidence — TUI regression coverage for lens-state transitions and projected rendering

Modified file:
- tests/test_tui.py

What the new tests cover:
- Shared lens defaults and the exact five-lens tuple `(dominant_field, limit_scored_events, group_dominant_field, limit_event_groups, only_matching_interventions)`.
- All five selected lens controls (`a/s/d/f/v`) through state helper coverage, including cycle order and toggle behavior.
- Cache invalidation on lens changes for both detail and compare panes without disturbing non-lens state such as selection, compare staging, focus, zoom, or active view.
- Exact kwargs passed into `get_run_summary()` and `compare_runs()`, including `None` defaults and active projected lens values.
- Header, footer, and help exposure of active lens state and bindings.
- Empty projected detail and compare renders as valid operator views rather than missing-data failures.
- Preservation of non-lens navigation behavior in the raw-key browser flow: help toggle, pane focus, zoom, compare activation, compare stepping, list navigation, and top/bottom jumps.

Semantic anchors reused instead of duplicated:
- `tests/test_history_show.py` already establishes that filtered single-run projections can legitimately return empty scored-event/event-group/intervention slices while still representing persisted truth. The TUI tests mirror that by asserting projected empty detail renders remain valid and by checking the TUI passes the same lens kwargs through to `get_run_summary()`.
- `tests/test_history_compare.py` already establishes that compare projections can legitimately produce empty filtered top-event/group views while preserving compare-side persisted truth. The TUI tests mirror that by asserting projected empty compare renders remain valid and by checking the TUI passes the same lens kwargs through to `compare_runs()`.
- The new TUI coverage does not restate storage filtering algorithms or diff semantics in depth; it verifies TUI state transitions, cache invalidation, and storage handoff fidelity.

Verification performed:
- `lsp_diagnostics` on `tests/test_tui.py`: no errors.
- `.venv/bin/python -m unittest discover -s tests -p "test_tui.py" -v`: PASS (43 tests, OK).

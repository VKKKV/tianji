Task 5 targeted verification note

Initial command attempted:
- `python3 -m unittest tests.test_scoring -v`

Outcome:
- failed with `ModuleNotFoundError: No module named 'support'`

Reason:
- `tests/test_scoring.py` imports `from support import *`, so this suite expects unittest discovery mode rooted at `tests/` rather than direct dotted-module invocation from repository root.

Follow-up:
- reran via discovery using `python3 -m unittest discover -s tests -p 'test_scoring.py' -v`
- result: pass

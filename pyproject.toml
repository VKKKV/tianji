[project]
name = "tianji"
version = "0.1.0"
description = "TianJi one-shot fetch infer backtrack MVP"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1.0",
    "rich>=13.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]

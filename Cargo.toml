[package]
name = "tianji"
version = "0.1.0"
edition = "2021"
description = "Rust migration scaffold for TianJi fixture artifact emission"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
